
####################################################################################################################################
################################## renabled ########################################################################################
# >>
#' @param renabled
#' (optional)\cr
#' If set to `FALSE`, the contents of the file(s) will be stored as a list in a (new) component of the ParaDRAM object
#' named `chainList` / `markovChain` / `sampleList` / `reportList` / `restartList` / `progressList`  and `NULL` will be the
#' return value of the method. If set to `TRUE`, the reverse will done.\cr
#' The default value is `FALSE`.
# <<
################################## renabled ########################################################################################
####################################################################################################################################
